version: "3.9"

services:

  fastapi:
    container_name: ${APP_NAME}
    image: ${APP_NAME}
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    env_file:
      - env/.env
    command: sh -c "
      ./scripts/run_app.sh
      "
    ports:
      - "8001:8000"
    healthcheck:
      test: [ "CMD-SHELL", "curl -fsSL http://localhost:8001/api/healthcheck/" ]
      interval: 10s
      timeout: 60s
      retries: 5
      start_period: 10s
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - ../backend:/home/unprivilegeduser/shortener

  postgres:
    container_name: postgres_user
    image: postgres:${POSTGRES_VERSION}
    restart: always
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME}
    ports:
      - "5432:5432"
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U ${DB_USER} -d ${DB_NAME}" ]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 5s
    deploy:
      resources:
        limits:
          cpus: "1"
          memory: 4G
    volumes:
      - ../persistentdata/db:/var/lib/postgresql/data/
